/**
 * 
 */
package usingArrayConcepts;
import java.util.*;

/**
 * 
 */
public class ArrayConcepts {

//	// pass in array and length of array
//	static void reverseArray(int array[], int length) {
//		
//		// create a new array object in memory that also contains the length of the array
//	    int[] arr = new int[length];
//		//used to keep track of the position
//	    //in the array where we will place elements in reverse order
//	    int len = length;
//		// loop through length of array
//		for (int i = 0; i < length; i++) {
//			
//			//len - 1 is used to place elements at the end of the array
//			//and move backwards. If len is 5, len -1 is 4, so the 
//			// element goes into position 4.
//			// array[i] is current element if the array
//			// so, we are shifting the current element we are on
//			// in the loop to the end by 1 position
//			arr[len - 1] = array[i];
//			// The length of the array is decreased by 1 now
//			// so, the next element in the list will be placed
//			// one position earlier in arr
//			// This ensures that the next element gets placed
//			// in the next available slot in reverse order
//			len = len - 1;
//			
//		}
//
//		// loop over length of array again
//		for (int j = 0; j < length; j++) {
//			
//			// print out the array
//			System.out.println(arr[j]);
//			
//		}
//
//	}
	
//	//using collection of objects to reverse array
//	static void reverseArray(Integer array[]) {
//
//		 Collections.reverse(Arrays.asList(array));
//		
//		 System.out.print(Arrays.asList(array));
//		
//		
//	}
//	
//	
	
	// Find Min / Max element in Array
	
	// Remove duplicate values in Array
	
	// Sort Array
	
	// Merge two sorted arrays
	
	
	public static void main(String[] args) {
		// TODO Auto-generated method stub
		// create array to pass in to method as first param
		//int a[] = {10, 20, 30, 40, 50};
		//Integer a[] = {10, 20, 30, 40, 50};
		// call .length on array for second param
		//reverseArray(a, a.length);
		//reverseArray(a);
	}

}
